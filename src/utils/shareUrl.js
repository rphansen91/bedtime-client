import { pluck } from 'rp-utils';

// const protocol = pluck(window,'location.protocol') || '';
// const domain = pluck(window,'location.host') || '';

module.exports = id => `https://bedbyestory.com/#/?id=${id}`